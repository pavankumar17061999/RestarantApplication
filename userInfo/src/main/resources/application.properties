# Server port to run user microservice
server.port=8891

# MySQL datasource connection details
spring.datasource.url=jdbc:mysql://localhost:3306/userdb
spring.datasource.username=root
spring.datasource.password=17Jk5@0110
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
# Optional: specify MySQL dialect, uncomment if needed
# spring.jpa.database-platform=org.hibernate.dialect.MySQL5InnoDBDialect

# Eureka client service URL for service registry
eureka.client.service-url.defaultZone=http://localhost:8762/eureka/

# Application name that registers in Eureka
spring.application.name=USER-SERVICE
